import type { PrintJS } from './types';
declare const printJS: PrintJS;
export default printJS;
export { printJS };
export type { Configuration, PrintType, PrintJS, PrintParams, JsonProperty, NormalizedJsonProperty } from './types';
